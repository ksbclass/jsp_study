<?xml version="1.0" encoding="UTF-8" ?>
<!--  /mybatisstudy/src/mapper/MemberMapper1.xml -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	<select id="count" resultType="int"> <!-- member.count : namespace.id 명 -->
		select count(*) from member
	</select>
	<select id="list" resultType="Member">
		select * from member
	</select>
	<select id="selectid" resultType="Member" parameterType="String">
		select * from member where id= #{value}
	</select>
	<select id="selectname1" resultType="Member" parameterType="String">
		select * from member where name like #{value}
	</select>
	<select id="selectname2" resultType="Member" parameterType="String">
		select * from member where name like '%${value}%'
	</select>
	<select id="selectNameGender" resultType="Member" parameterType="map">
		select * from member where name like '%${name}%' and gender = #{gender} 
	</select>
<!-- 
${name} : map 객체 중 key 값이 name 인 객체의 값 리턴
#{gender} : map 객체 중 key 값이 gender 인 객체의 값 리턴

#{value} : value 값 + 자료형 인식
		   value 의 값이 가 가정할때
		   where name like %#{value}%  => 오류 발생
		   where name like %'가'% 	   => 오류 발생  
'%${value}%' or "%${value}%" : 둘중에 아무거나 사용해도 상관없다. value 값 인식
		   value 의 값이 가 가정할때
		   where name like '%${value}%'  
		   where name like '%가%'	   => 정상처리  
 -->
</mapper>